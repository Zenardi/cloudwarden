"""Kubernetes cost & governance (M14.12).

Managed-Kubernetes (AKS/EKS/GKE) discovery, workload inventory and usage behind
injectable clients — the same seam the cloud cost collectors use, so mock mode
replays recorded fixtures and no live cluster is required. Namespace cost
allocation and workload right-sizing live in ``analysis.k8s_rightsizing``.
"""
