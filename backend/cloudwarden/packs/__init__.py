"""Bundled policy packs (M10.1).

Curated, versioned Cloud Custodian policies shipped as YAML under this package.
:mod:`cloudwarden.packs.registry` discovers them and installs a pack into a
policy collection (Stacklet's out-of-the-box policy packs).
"""
