# Test/dev tooling (not needed at runtime).
-r requirements.txt
pytest>=8.3
pytest-cov>=5.0
ruff>=0.6
testcontainers[postgres]>=4.8
mutmut==3.6.0  # mutation-testing gate on core modules (issue #52). Pinned: 2.x's pony-ORM cache breaks on Python 3.13.
